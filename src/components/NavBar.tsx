import { h } from "preact";

const NavBar = () => (
	<header>
		<nav>
			<a href="/">Home</a>
			<a href="/lazy">Lazy</a>
			<a href="/about">About</a>
		</nav>
	</header>
);

export default NavBar